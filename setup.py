from setuptools import setup, find_packages

setup(
    name='diamond_price_predictor',
    version='0.0.1',
    author='D-Cyberguy',
    packages=find_packages(),
    install_requires=[]      # requirements.txt handles this
)